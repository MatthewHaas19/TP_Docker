buildscript {
    repositories {
        gradlePluginPortal()
    }
    dependencies {
        classpath('com.bmuschko:gradle-docker-plugin:6.6.1')
    }
}

apply(plugin = "com.bmuschko.docker-remote-api")
